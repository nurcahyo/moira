{{- include "moira-console.validate" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moira-console.fullname" . }}-config
  labels:
    {{- include "moira-console.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
