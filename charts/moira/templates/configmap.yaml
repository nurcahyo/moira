apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moira.fullname" . }}-config
  labels:
    {{- include "moira.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
