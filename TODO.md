# TODO

Hardening items, leftovers, deferred follow-ups and known gaps surfaced while executing the
iteration plans. Nothing here is silently dropped: an item that is not done lives here, and an item
needing a human decision lives in [`NEED_CONFIRMATION.md`](./NEED_CONFIRMATION.md). The evidence
behind each of those decisions — what the runner recommended, whether it was executed, and how to
reverse it — lives in [`docs/decisions-taken.md`](./docs/decisions-taken.md); note that none of them
has been confirmed by a human yet.

Format: `- [ ] <item> — source: <plan> — <why deferred>`

An item that is still open and now has a GitHub issue carries that issue number inline, so the
two cannot drift apart. A closed item keeps its original text and gains a **Closed by** clause
naming the commit or the file that closes it — nothing is deleted, because the reason an item
existed is part of the record.

## From plan 02a (MVP boundary honesty)

- [x] Wire `pub mod i18n;` into `src/lib.rs` and make the catalog load-bearing (finding P0-5) — source: plans/02a-mvp-boundary-honesty.md — `src/i18n/` is orphaned, so `moira::i18n::is_known_key` is neither compiled nor reachable from `tests/`. 02a adds zero keys so it had nothing to assert; the plan assigns wiring to 02b Wave 0 / plan 04 Wave 0 and the enforcement test to plan 05. **Closed:** `src/lib.rs:9` is `pub mod i18n;`, and `moira::i18n::is_known_key` is now the assertion in fourteen integration binaries — `tests/http_error_contract.rs`, `http_middleware_contract.rs`, `admin_query_contract.rs`, `list_pagination.rs`, `execution_policy_if_match.rs`, `jwks_hardening.rs`, `idempotency_hash_migration.rs`, `metrics_endpoint.rs`, `cluster_coordination.rs`, `content_leak_snapshots.rs`, `secret_leak_snapshots.rs`, `rag_ingestion_honesty.rs`, `rag_idempotency_replay.rs`, `worker_queue.rs`. Still open, and tracked separately below and in **#99**: the `docs/i18n-response-catalog.json` mirror has no automated drift assertion.
- [x] Implement real `Idempotency-Key` replay on the four RAG write routes — source: plans/02a-mvp-boundary-honesty.md — deliberately deferred to plans/02b-idempotency-replay.md per CONVENTIONS.md §0 decision D1/D2. Until it lands the parameter is accepted and ignored, which 02a states honestly in the spec and docs. **Closed by plan 02b, commit `e1c2658` (PR #24):** `rag.collection.create`, `rag.document.create` and `rag.document.ingest` (shared with `/reindex`) run through `AdminCommandRunner`; the contract is in `docs/idempotency.md` and the suite is `tests/rag_idempotency_replay.rs`.
- [x] Delete Sentence B and the `rag_write_routes_carry_the_interim_idempotency_disclaimer` unit test — source: plans/02a-mvp-boundary-honesty.md — they exist only to describe the interim window between 02a and 02b. 02b's Definition of Done includes removing them, and replacing `repeated_ingest_with_the_same_idempotency_key_creates_two_versions_until_02b` with its inverse. **Closed by plan 02b, commit `e1c2658`:** the unit test is now `rag_write_route_descriptions_no_longer_disclaim_idempotency` (`src/http/mod.rs:1570`), which asserts Sentence B is *absent* and Sentence A survives verbatim; the e2e test is now `repeated_ingest_with_the_same_key_replays_exactly_one_indexed_version` (`tests/rag_ingestion_honesty.rs:891`).
- [ ] Document that admin routes still advertise unimplemented replay (#92) — source: plans/02a-mvp-boundary-honesty.md — `docs/idempotency.md`'s new paragraph names only conversation/memory/RAG, but `PUT .../execution-policy`, `PUT .../users/{external_user_id}/provider-credentials/{id}`, `POST .../routes`, `POST .../routing-policies`, `POST .../agent-profiles` and `PUT .../providers/{provider_id}/runtime-policy` also declare the header without replaying. Incompleteness rather than falsehood; `docs/todo.md` Phase 2 tracks the slice and 02b should close it. **Partly overtaken, re-counted against the current tree.** Sixteen admin routes declare `Idempotency-Key` (`grep -n 'Idempotency-Key' src/http/admin.rs`). Ten replay atomically through `AdminCommandRunner` and are in `docs/idempotency.md`'s table. **Four** more do replay, through `RuntimeAdminService`'s older non-atomic scheme, and are named in no doc: `route.create`, `routing_policy.create`, `agent_profile.create` and `provider_runtime_policy.upsert`. Those are all four hits of `git grep -n 'idempotency_replay(' src/application/runtime_admin.rs` — `:76`, `:263`, `:478`, `:697` — and each returns the replayed record early. `src/application/runtime_admin.rs:1377-1381` names the same four independently, as the set of calls that could replay another actor's response under the pre-plan-06 fingerprint. `PUT .../users/{external_user_id}/provider-credentials/{id}` delegates to `create_credential` (`src/http/admin.rs:958-966`) and therefore replays as `credential.create`. The delegation is real, but `docs/idempotency.md`'s operation table maps `credential.create` to `POST /api/v1/admin/provider-credentials` only — the user-credential `PUT` appears nowhere in it, so a reader cannot learn that the two routes share a replay identity and that one `Idempotency-Key` therefore collides across both. The *route* is still undocumented even though the *operation* is; that line belongs in the same doc fix. That leaves **one** route that declares the header and genuinely never replays: `PUT /api/v1/admin/applications/{id}/execution-policy` — `put_application_execution_policy` (`src/application/public.rs:854`) reaches its repository with no ledger claim of any kind. 10 + 4 + 1 + 1 = 16. The doc gap is real for the same five routes either way — the re-count moves `provider_runtime_policy.upsert` from the never-replays column into the non-atomic-replay column, it does not change the set — so five is the count of routes **#92** owns for both the doc and the atomicity of the runtime-admin sites.
- [ ] Add an authn/authz negative test on a RAG route (#99) — source: plans/02a-mvp-boundary-honesty.md — the new e2e suite sends no credentials and relies on `admin_settings.enabled = false` yielding a `DevAdmin` actor, so a regression stripping auth from these routes would not be caught by it. Security review proved 02a changes auth byte-for-byte-not-at-all (126 `security(...)` annotations and 209 guard call sites identical), so this is a pre-existing coverage gap, not one 02a introduced.
- [x] Revisit `RagIngestionStatus::Superseded`, now unreachable — source: plans/02a-mvp-boundary-honesty.md — the supersession `CASE` only rewrites `'indexed'` into `'superseded'`, and nothing reaches `'indexed'` any more, so superseded versions keep `'pending'`. **Closed by plan 11:** `'indexed'` is reachable and earned, so the `CASE` fires again; `reindex_supersedes_the_previous_version_and_indexed_is_earned_not_laundered` covers it.
- [x] Tighten `public_response_schema_documents_always_empty_citations` — source: plans/02a-mvp-boundary-honesty.md — it matches lowercased substrings (`"empty"`, `"not wired"`) while every sibling OpenAPI test matches verbatim constants, so it would survive prose drift. **Closed by F31, and the item had already been overtaken twice.** First, the test's real defect was not looseness but *direction*: it asserted the description claimed retrieval was unwired, which plan 11 made false, so the loose matching was the least of it — it is now inverted as `public_response_schema_documents_citations_as_real_provenance`. Second, the verbatim pin this item asked for already exists: plan 05's `committed_openapi_matches_the_generated_document` compares `docs/openapi.json` byte-for-byte, so every description is pinned verbatim and adding a third copy of the string to a unit test would duplicate a gate rather than add one. The inverted test deliberately asks the *complementary* question the snapshot cannot — whether the prose is true — which is why it stays substring-based.
- [ ] Give `rag_document_select(inner: &str)` a compile-time contract (#99) — source: plans/02a-mvp-boundary-honesty.md — every one of its five call sites passes a string literal today (verified), but the signature cannot enforce that, so a future dynamic call site would introduce SQL injection without any type-level objection.
- [x] Keyset pagination on the RAG list endpoints (finding P1-4) — source: plans/02a-mvp-boundary-honesty.md — assigned to plan 04. Note that plan 04 must build on the outer `order by document_rows.created_at desc, document_rows.id desc` that 02a restored in `rag_document_select`; the ordering it depends on was briefly broken by this branch's join. **Closed by plan 04, commit `400ad70` (PR #26):** `tests/list_pagination.rs:1291` `rag_document_list_pages_through_the_cursor_it_advertises` walks the whole cursor against the SQL ordering it asserts (`expected_rag_document_order`, `:919`). The *public* lists are a separate, still-open item below (**#93**).
- [ ] Automated drift gating for the honesty text 02a wrote (#99 for the i18n mirror) — source: plans/02a-mvp-boundary-honesty.md — OpenAPI drift is plan 05, i18n catalog ↔ JSON mirror drift is plan 06. Until then the only protection is `conversation_memory_rag_operations_document_the_mvp_preview_boundary` plus review. **Half closed:** the OpenAPI half landed with plan 05 — `docs/openapi.json` is committed and `tests/openapi_drift.rs` fails the build on divergence. The `docs/i18n-response-catalog.json` mirror still has no automated assertion; **#99** names it as the one item on that checklist that is confirmed open and directly workable.
- [x] Full RAG/memory pipeline: chunking, embeddings, retrieval, context injection, real citations — source: plans/02a-mvp-boundary-honesty.md — plan 11. **Landed in full: Sub-Phases A–G.** Summarization (E) was the last gap and it landed in `dac7468` (PR #52) — `src/application/summarization.rs`, `src/infra/repositories/conversation.rs`, and the 1,177-line `tests/conversation_summarization.rs`. The instruction "keep its wording until plan 11's behaviour is verified" was followed and then never revoked: plan 11 verified the behaviour without retiring the wording, so twelve OpenAPI descriptions kept advertising a no-op. F31 retired it. A "keep X until Y" item needs its retirement listed as part of Y, not only as a condition on X.

## From plan 02b (idempotency replay)

Surfaced by four adversarial QA lenses (correctness, security, test-integrity, conventions). Items
fixed inside 02b are not listed here; these are the ones deliberately left for a later plan.

- [x] Unify `runtime_admin.rs`'s divergent `actor_fingerprint` with `admin.rs`'s — source: plans/02b-idempotency-replay.md — `src/application/runtime_admin.rs:702` fingerprints only `(actor_type, subject, api_key_id)`, omitting `trusted_jwt_issuer_id`, `internal_application_id`, `application_id`, `tenant_id`, `external_user_id`, `external_tenant_id` and `delegated_subject`, while writing into the **same** `idempotency_records` table as the 10-field `admin.rs:1375` version. Proven empirically: three actors differing only by issuer / application / tenant all hash to `oBmwFVvCAAAh` under the runtime copy. Two `TrustedJwt` actors sharing a `sub` across different registered issuers can therefore replay each other's `POST /api/v1/admin/routes`, `routing-policies`, `agent-profiles` or `provider-runtime-policy` responses. **Pre-existing on `main`; 02b explicitly excludes `runtime_admin.rs` and plan 06 owns the unification.** 02b's routes are unaffected because `operation` is part of the unique index and the `rag.*` operations are disjoint. **Closed by commit `27fe021`, "unify the actor fingerprint across admin, runtime-admin, and public":** the crate now has exactly one definition, `src/application/admin/shared.rs:344`, which `runtime_admin.rs:14` imports; `tests/actor_fingerprint_unification.rs` is the regression guard and `shared.rs:1130-1183` pins that actors differing only by issuer, tenant or delegated subject no longer collide. Residual, deliberate and read-only: `legacy_actor_fingerprint` (`src/application/runtime_admin.rs:1042`) survives as a dual-**read** lookup so in-flight ledger rows written under the 3-field formula stay replayable — its removal is the separate item below.
- [ ] Remove the legacy `actor_fingerprint` fallbacks after the plan-06 deploy — source: plans/02b-idempotency-replay.md (via plan 06) — **needs an owner and a date; this is the scheduling issue #96 asked for.** Four `TODO(post-deploy)` markers: `src/application/runtime_admin.rs:938` and `:1039`, `src/application/public.rs:1060` and `:2239`. **Trigger:** the date plan 06 reaches production, plus 24 hours — `idempotency_records.expires_at` is set 24h ahead, so that is when the last ledger row written under the old fingerprint expires. Gated on a **deploy**, not a merge, which is why no plan owns it. Removing it early means a client retrying an idempotent request across the deploy boundary misses its ledger row and **executes the side effect a second time**; removing it late costs only dead code. If in doubt, wait longer. See [`docs/decisions-taken.md`](./docs/decisions-taken.md) §5.
- [x] Prune the idempotency ledger — source: plans/02b-idempotency-replay.md — plan 04's retention worker is the designated pruner. `WorkerRegistry`'s `"retention-cleanup"` spec currently has no execution body, and the opportunistic purge in `claim_idempotency` only deletes rows for the *same* `(key_hash, fingerprint, operation)`, so rows under distinct keys are never reclaimed. Worse than incidental growth: deterministic-failure caching (`src/application/admin_command.rs:210-235`) commits a ledger row for a *rolled-back* cacheable 4xx, so an actor holding only `moira:rag-documents:ingest` can mint one row per request by ingesting non-existent document ids under fresh keys — cheaper than a real mutation and directly attacker-controllable. **If plan 04 slips, 02b ships unbounded growth.** **Closed by plan 04, commit `400ad70` (PR #26):** `src/infra/workers/retention.rs` is the worker body, `idempotency_records` is its first named target (`:5`, `TABLE_IDEMPOTENCY_RECORDS` at `:97`), victim selection rides `idempotency_records_expires_at_idx`, and `tests/retention_worker.rs` covers it. Still open below, and unaffected by this: the *public* ledger's expired-row sweep before lookup.
- [ ] Bound the advisory-lock poll against the connection pool (#92) — source: plans/02b-idempotency-replay.md — `claim_idempotency` (`src/infra/repositories/admin.rs:563-581`) polls `pg_try_advisory_xact_lock` every 20ms for up to 5s *inside an already-open transaction on a pooled connection*, and `config/default.toml:10` sets `max_connections = 10`. Ten concurrent duplicate-key requests pin the whole pool for five seconds; the eleventh request on *any* route, including `/v1/responses`, blocks on pool acquire. `ingest` additionally now holds its `for update` document-row lock for the full claim→finalize window. Pre-existing primitive, but 02b lowers the bar for reaching it.
- [ ] Bound `Idempotency-Key` length and charset (#92) — source: plans/02b-idempotency-replay.md — `src/application/context.rs:30` takes the header verbatim, unlike `runtime_admin.rs:722`'s `validate_key` (1–128 chars). Nothing overflows since the key is hashed to a fixed-width column, so this is hygiene rather than a hole.
- [ ] Cover the advisory-lock **timeout** arm of `idempotency_in_progress` for the RAG routes (#99) — source: plans/02b-idempotency-replay.md — `in_progress_claim_returns_a_catalogued_idempotency_in_progress_error` forces the *unfinalized-record* arm with direct SQL, and the concurrency test's loser always waits and replays rather than timing out, so the 5s-deadline arm (`src/infra/repositories/admin.rs:574-579`) is exercised only by `advisory_lock_timeout_is_transient_and_does_not_mutate_or_cache` on the admin path. The code path is shared, so this is a coverage gap rather than a hole.
- [ ] Assert the `docs/i18n-response-catalog.json` mirror automatically (#99) — source: plans/02b-idempotency-replay.md — the two catalog tests assert the **Rust** side only; the JSON mirror was verified by hand (64 entries, 62 unique, the two pre-existing duplicates `idempotency_conflict` and `rate_limited` left alone per instruction). CONVENTIONS §4 rule 4 defers the drift test to plan 06, so this is compliant, not a violation.
- [ ] Decide whether a replayed request should leave an audit trace (#92) — source: plans/02b-idempotency-replay.md — the replay branch (`src/application/admin_command.rs:175-178`) returns before the mutation closure, so no `audit_logs` row is written. Before 02b every RAG write produced one; now a repeated keyed request leaves no record that a second request was received. Defensible, and pinned by the concurrency test, but it is an observable behavioural change.
- [ ] Make `AdminCommandOutcome.status` load-bearing (#92) — source: plans/02b-idempotency-replay.md — all three service methods return `Ok(outcome.response)` and discard `outcome.status`, `outcome.resource_id` and `outcome.replayed`; the handlers re-derive the status as a hardcoded literal. Stored and hardcoded values coincide today, so replay is verbatim by coincidence rather than by construction. The moment any of these operations gains a conditional status (e.g. ingest returning `202` once plan 11 lands) replay will silently return the wrong one. `src/application/public.rs:179` already honours `outcome.status` on its own path. Same pattern exists in `admin.rs`, so pre-existing.

## From plan 03 (security hardening)

Surfaced by four adversarial QA lenses (SSRF/JWKS, hashing migration, middleware/availability,
test integrity). Items fixed inside 03 are not listed here; these are deliberately deferred.

- [x] Pepper `actor_fingerprint` too (#95) — source: plans/03-security-hardening.md — it is the one column in `idempotency_records` still produced by the unkeyed `secret_fingerprint`, in all three producers (`src/application/admin.rs`, `src/application/public.rs`, `src/application/runtime_admin.rs`). P1-1's own threat model applies *harder* here than to request bodies: a JWT `sub` / `external_user_id` / email is far lower-entropy than a JSON body, and once an attacker knows the actor type the tuple is enumerable. Plan 03 scoped only `idempotency_key_hash`/`request_hash`/`content_hash`, so this is out of scope by the letter — but the table is not actually hardened until it is done, and **plan 07 is what fills this column with human identity**. Doing it after 07 needs the same dual-read/dual-lookup dance, since `actor_fingerprint` is part of the unique index. Cheapest while the scaffolding is fresh. **Still open, but smaller than written:** commit `27fe021` collapsed the three producers into one, so this is now a single-function change at `src/application/admin/shared.rs:344` plus the dual-read window that `legacy_actor_fingerprint` already established as a working pattern. `src/application/admin.rs` no longer exists — it is the `src/application/admin/` module directory. **Closed by issue #95:** `actor_identity_bytes` (`src/application/admin/shared.rs`) is now the shared pre-image and `actor_fingerprint` reduces it through `IdempotencyHasher`, so the stored value carries the `"{pepper_version}:"` prefix and is no longer derivable from the actor's identity alone. It is **not** a one-line edit and was not shipped as one: the fingerprint is a column of the `idempotency_records` unique index, so a pre-deploy row sits at an index point the peppered value cannot address. All three read paths therefore gained an `unkeyed_actor_fingerprint` probe — the admin-command claim (`AdminIdempotencyClaim::legacy_actor_fingerprint`), `runtime_admin::idempotency_replay`, and `public::claim_idempotency` — while the write path emits only the peppered value, so no legacy value can re-enter the ledger. No migration: the column is `varchar(128)` and the value is 46 characters. Guarded by `an_unkeyed_fingerprint_row_still_replays_after_the_pepper_switch`, `an_unkeyed_fingerprint_row_still_replays_through_the_admin_command_claim` and `every_idempotent_command_path_writes_a_peppered_fingerprint` in `tests/actor_fingerprint_unification.rs`. The new probes join the existing `TODO(post-deploy)` removal window below on their own 24h clock.
- [x] Bound stale-JWKS retention in time (#95) — source: plans/03-security-hardening.md — `src/security/auth.rs` serves a retained cache entry on *any* fetch failure with no maximum staleness, and neither `entries` nor `locks` is ever evicted. A key revoked at the IdP stays trusted for as long as the endpoint keeps failing — indefinitely. The right availability call short-term, but it converts "DoS the IdP's JWKS endpoint" into "a compromised signing key can never be rotated out". Wants a `max_stale_seconds` ceiling plus entry eviction; both maps are also keyed by admin-controlled URL strings. **Closed by issue #95 for the `entries` map:** `JwksFetchSettings::max_stale_seconds` (default 24h) bounds the retention, `CachedJwks` tracks `stored_at` separately from `expires_at` so freshness and staleness are limited independently, and `JwksCache::retained` evicts the entry and fails closed past the ceiling rather than merely declining to serve it. `a_cached_jwks_past_the_stale_ceiling_is_evicted_and_refused` asserts both sides of the boundary — a bound with only a far-side assertion would also be satisfied by refusing unconditionally, which would break the availability property the retention exists for. **Still open:** the `locks` map is still unbounded. It holds one `Arc<Mutex<()>>` per distinct admin-configured JWKS URL and no key set, so it is a slow memory-growth item rather than a trust one, and it is deliberately left for its own change.
- [ ] Key the idempotency advisory lock on a migration-stable fingerprint (#95) — source: issue #95 review — `claim_idempotency` (`src/infra/repositories/admin.rs`) derives its `pg_try_advisory_xact_lock` key from the *current* `actor_fingerprint`, and that spelling changed when the fingerprint was peppered. During a rolling deploy two instances on opposite sides of the rollout derive different lock keys for the same actor and `Idempotency-Key`, so they do not mutually exclude; because they also insert at different points of the `(idempotency_key_hash, actor_fingerprint, operation)` unique index, both can execute rather than one replaying the other. The window is the rollout itself, not the 24h retention. The fix is to derive the lock key from a value that is stable across the switch — the lock key is never stored, never returned and never compared against a ledger column, so it does not need to be peppered. Deliberately not folded into the #95 fix: `advisory_lock_timeout_is_transient_and_does_not_mutate_or_cache` (`tests/admin_idempotency.rs`) pins the current derivation, so this needs its own change and its own test rather than a quiet edit to an existing expectation.
- [ ] Retire the legacy-hash acceptance window — source: plans/03-security-hardening.md — plan 03 adds `idempotency.accept_legacy_hashes` (default `true`). Flip it to `false` after one full 24h retention period has elapsed post-deploy, which also drops the unconditional extra `SELECT` on every idempotent `/v1/responses` request. **Scope, stated because it was briefly wrong:** this switch governs the plan-03 *key-hash* window only. The `actor_fingerprint` dual-read added by #95 is a separate, later window on its own 24h clock and is deliberately not gated on this flag — a deployment that had already closed this one would otherwise have received the peppered fingerprint with no dual-read at all, making every pre-pepper ledger row unreachable and duplicating idempotent commands across that deploy. `an_unkeyed_fingerprint_row_replays_even_with_the_key_hash_window_closed` pins the independence. **Blocked on the item below:** on the admin-command path the flag cannot be flipped at all today, because nothing reads it into the runner.
- [ ] Wire `idempotency.accept_legacy_hashes` into the admin-command path (#95) — source: issue #95 review — `AdminCommandRunner::accepting_legacy_hashes` (`src/application/admin_command.rs`) is the only way to set the runner's `accept_legacy_hashes`, and a grep over `src/` and `tests/` finds its definition plus exactly one caller, `tests/actor_fingerprint_unification.rs`. Every production construction site goes through `AdminCommandRunner::new`, which hardcodes `true`, so the operator setting reaches `Settings` and stops there: the plan-03 key-hash window on this path is permanently open regardless of configuration. Consequences are cost and reach, not a hole — an extra `SELECT` per idempotent admin write, and unkeyed stored digests stay acceptable on read past the date operators were told they would stop being. Pre-existing since plan 03 (`19b98ae`), not introduced by the #95 fix, which only inherited it; the #95 fix does make it visible by documenting a switch that does not move. The change is to thread `Settings` through the twelve construction sites and chain the builder, with a test that a runner built the production way honours a `false` setting.
- [x] Redact `Debug` on pepper-bearing types (#95) — source: plans/03-security-hardening.md — `IdempotencyHasher` derives `Debug` over `pepper: Vec<u8>`, `AdminCommandRunner` derives `Debug` while holding it, and `IdempotencySettings` derives `Debug` over `pepper_base64`. **No active leak today** — a reviewer grepped every `{:?}`/`?state`/`?settings` path reaching them and found none — but `src/application/conversation.rs` already uses `format!("{spec:?}")` in the same area, so the idiom is live. Custom `Debug` impls would make the invariant structural rather than incidental. `ApiKeyHasher` has the identical exposure; plan 03 copied a pre-existing pattern rather than introducing a regression. **Closed by issue #95:** hand-written `Debug` on `IdempotencyHasher`, `ApiKeyHasher`, `IdempotencySettings` and `ApiKeySettings`. The pepper version and whether a pepper is configured stay visible — both are operationally load-bearing and neither is the secret; only the pepper itself is replaced by `[redacted]`. Note the issue's own checklist guessed `api_keys.rs` was already custom-`Debug`; it was not. What was already redacted there is `GeneratedApiKey::raw_key` via `SecretString`, a different secret on a different type, and the hasher's pepper sat behind a plain derive reachable from `AuthService`. Pinned by `debug_redacts_the_pepper_everywhere_it_is_reachable` (`src/security/idempotency.rs`), which is a **wiring** test, not a predicate one: it walks the real containers — `AdminCommandRunner`, `AuthService`, `Settings` — because each derives `Debug` and is therefore only as safe as the leaf it holds, and that dependency is invisible from the leaf. It also searches for the pepper in both the text *and* the byte-array spelling, since `#[derive(Debug)]` on a `Vec<u8>` prints `[105, 100, …]` and a text-only assertion would have stayed green against the exact regression it guards.
- [ ] Sweep expired rows on the public idempotency ledger (#92) — source: plans/03-security-hardening.md — the admin transaction sweeps both key hashes before lookup; the public path has none. `claim_idempotency` does `on conflict do nothing` then reads with `expires_at > now()`, so when the conflicting row is expired the read returns `None` and the caller gets a **permanent** `409 execution_in_progress` for that key. Pre-existing; plan 03 rewrote the function and left it. Plan 04's retention worker is the natural owner. **Still open after plan 04.** `src/infra/workers/retention.rs` now deletes expired `idempotency_records` on a schedule, which shortens the window but does not close it: between two sweeps an expired row is still present, still wins the `on conflict do nothing`, and still produces the permanent `409`. The fix is in `claim_idempotency`, not in the worker.
- [x] Document that `CatchPanicLayer` does not cover streaming-body panics — source: plans/03-security-hardening.md — tower-http wraps only `inner.call(req)` and the response future, not the response body. Measured: `/internal/test/panic` returns a clean 500 envelope, but a panic *after* the first SSE frame returns zero bytes and a bare connection close. Latent today (Moira's SSE handler contains no `unwrap`/`expect`/`panic!`), but any future one truncates a stream indistinguishably from a network cut, with no terminal event and no 500. **Closed:** `src/lib.rs:72-93`, the doc comment on `build_router`, carries the whole gap — the measurement, why it is latent rather than live, why the two candidate fixes were rejected, and the invariant that replaces them ("no `unwrap`/`expect`/`panic!`/slicing/indexing in any code reachable from a streamed item").
- [x] Re-run plan 03's gates against PostgreSQL 16 in CI — source: plans/03-security-hardening.md — all four lenses independently assessed the PG18.3 deviation as not weakening any of their findings (every finding is application-layer: Rust control flow, HTTP status codes, in-process cache locking). The one test where a lock-manager difference could plausibly matter is `concurrent_same_key_claims_still_produce_one_ledger_row`. **Closed by CI:** `.github/workflows/ci.yml` runs the suite against the `pgvector/pgvector:pg16` service with `MOIRA_TEST_DATABASE_URL` exported, so the DB-backed tests actually execute rather than early-returning, and the six most recent `ci` runs on `main` all report `success` (`gh run list --branch main`). Not `develop`: the workflow's `push` trigger lists `main` only (`.github/workflows/ci.yml:5-7`), and `gh run list --branch develop` returns an empty set — there has never been a `ci` run on that branch, so no claim can be made about it. The residual PG16 question — re-measuring the pagination `EXPLAIN` numbers, which CI does not do — stays open below and in **#99**.

## From plan 04 (durability & correctness)

Surfaced by four adversarial QA lenses (pagination correctness, security, test integrity, conventions).
Items fixed inside 04 are not listed here.

- [x] Close the lost-update window on the other ~28 versioned admin mutations — source: plans/04-durability-correctness.md — plan 04 closed the TOCTOU on `PUT .../execution-policy` with a SQL-level version check, because that endpoint is the one it owns. The **same** check-then-act shape is used by roughly 28 `ensure_version(service.get_X(…).version, require_if_match(…))` call sites in `src/http/admin.rs`: read the version on one pooled connection, compare in Rust, then issue a separate unconditional write with no `where version = $n` and no row lock. Every one of those has the identical window. A reviewer proved the execution-policy case empirically — two writers holding the same currently-valid `If-Match`, released through a barrier, produced `successes=2 conflicts=0` across 5/5 runs. `rotate_credential` (`src/infra/repositories/admin.rs`) already does it correctly with `select … for update` inside a transaction, so the pattern to copy exists. **Plan 06 owns this** — it is a wide change with its own test surface, which is exactly why 04 did not attempt it. **Closed by plan 06b, commit `498221a`, "make `If-Match` atomic on all 33 admin mutation sites"** (the red test that proved it first is `ed03fad`). The true site count was 33, not ~28. `tests/if_match_toctou_harness.rs` was the failing-first evidence and is now the regression guard; `tests/if_match_family_races.rs` races the family. Scope note, so "all 33" is not over-read: this item counted `ensure_version(…)` call sites, and `PUT .../providers/{provider_id}/runtime-policy` was never one of them — it reads the header through `optional_if_match` (`src/http/admin.rs:2189`) and compares in Rust inside the service (`src/application/runtime_admin.rs:712-726`). `498221a` did not touch it, and it is still both optional and non-atomic; `docs/todo.md`'s Phase 2 entry carries the detail and **#92** owns it.
- [x] Paginate the public execution and usage lists (#93) — source: plans/04-durability-correctness.md — `/v1/executions` and `/v1/usage` still advertise a `cursor` they ignore (`ExecutionQuery`/`UsageQuery`, `list_executions_authorized`/`list_usage_authorized`). Identical bug to P1-4 and it reuses the same `src/domain/pagination.rs` codec, so it is a small fast-follow; plan 04 excluded it explicitly because those files were being edited by plans 03/05 concurrently. **Closed.** All four public lists now page on plan 04's mechanism, not a second one: `KeysetTail`/`bind_cursor`/`over_fetch_limit` moved out of `src/infra/repositories/admin.rs` into the shared `src/infra/repositories/keyset.rs`, and the assembly goes through `admin::shared::paginate`. Two lists were worse than described — `/v1/models` and `/v1/routes` had no `cursor` parameter *at all* and a hard-coded limit of 200, so row 201 was unreachable; both now take `cursor`/`limit` and both changed ordering to `created_at desc, id desc` (from `provider_models.id asc` and `route_key asc` respectively), because `ListCursor` is a `(timestamptz, uuid)` pair and a text key cannot be expressed in it. Migration `0026` adds the two unscoped cursor indexes `responses` and `usage_records` lacked.
- [x] Add page-through coverage for the remaining list endpoints (#93) — source: plans/04-durability-correctness.md — e2e walk tests exist for applications, audit-events, routes and conversations/messages. `providers`, `provider_models`, `credentials`, `user_credentials`, `system_keys`, `consumer_keys`, `trusted_jwt_issuers`, `memories`, `rag_collections` and `rag_documents` have none. That gap is precisely why a broken HTTP seam sat partially green: coverage existed *around* the seam, not through it. **Closed** (`rag_documents` was already covered before this). `tests/list_pagination.rs` now walks all nine remaining admin lists plus the four public ones to exhaustion, asserting every seeded row is seen exactly once against an order read back from Postgres. Timestamp collisions are seeded deliberately in five of them — `responses`, `usage_records`, `provider_models`, `consumer_api_keys` and `memory_records` — because a cursor keyed on the timestamp alone is only wrong when rows share one, and `usage_records.occurred_at` defaults to the transaction timestamp so that case is routine rather than exotic.
- [ ] Re-verify the pagination index choices on PostgreSQL 16 (#99) — source: plans/04-durability-correctness.md — the `EXPLAIN` numbers written into `src/infra/repositories/conversation.rs` justifying the new indexes were measured on 18.3 and are cited as such. Every *finding* in plan 04's review is version-independent, but the index selection itself is not.
- [ ] Consider lowering the retention batch-size default (#99) — source: plans/04-durability-correctness.md — the sweep takes `for update skip locked`, so it steps over rows a claim has locked; but the claim path's own opportunistic delete is a plain `DELETE` with no `skip locked`, so a claim CAN block behind an in-flight sweep batch. Bounded by one batch, but `MAX_BATCH_SIZE` is 10,000 and a 500-row batch measured 12.7s before the new indexes landed. The claim's 5s deadline covers only advisory-lock acquisition, not this delete.

## Cross-cutting / infrastructure

- [x] Make DB-dependent test suites fail loudly when the database is absent locally (#77) — source: plans/02a-mvp-boundary-honesty.md — with `MOIRA_TEST_DATABASE_URL` unset and `CI` unset, all e2e tests return early and report `ok` in ~0.01s having proven nothing, so the literal CONVENTIONS §2 `cargo test --workspace --all-features` gate is fully green on a machine with no database. The `CI=true` path does panic correctly. Systemic, inherited from `tests/support/mod.rs`, not introduced by 02a; until it changes, PR test evidence must show the DB-backed run, not merely a green transcript. **Closed.** The absence of a database is now a panic at every resolution point — `tests/support/mod.rs::database_origin`, `src/test_support.rs::database_origin`, `tests/security_foundation.rs::migration_test_database_url` — with `MOIRA_TEST_ALLOW_NO_DATABASE=1` as the single deliberately-named opt-out, refused when `CI=true`. Measured: `cargo test --test retention_worker` with the variable unset now reports `0 passed; 8 failed`. Two further things were found while closing it: the skip lines the gate greps for were being swallowed by `libtest`'s output capture and never reached the log at all (they are now written straight to `std::io::stderr()`, and the same measurement went from **0** matches to **1**), and the same silent-skip shape existed at nine call sites in `src/**/tests`, which now go through `crate::test_support`.
- [x] Re-run the 02a gates against PostgreSQL 16 in CI — source: plans/02a-mvp-boundary-honesty.md — local verification used PostgreSQL 18.3 because Docker is unavailable on the dev machine. **Closed by CI, same evidence as the plan-03 entry above:** `.github/workflows/ci.yml` pins the `pgvector/pgvector:pg16` service and exports `MOIRA_TEST_DATABASE_URL`, and recent `ci` runs on `main` report `success`. [`docs/decisions-taken.md`](./docs/decisions-taken.md) §2/6 records the same evidence and explicitly leaves closing this line to whoever syncs `TODO.md` next — that is this line closing. Its "Confirmed?" column stays **not yet** regardless: that column tracks a maintainer's sign-off on the broader policy question (is PG18-local/PG16-CI verification acceptable going forward), which is a separate, still-open decision that checking this box does not settle. `develop` is not a `push` trigger branch and has no `ci` runs of its own, so no claim is made about it here either.
