# Retrieval Ranking

Retrieval policy fields model semantic, keyword, recency, and importance weights.

The current implementation persists policy and retrieval-run metadata but does not yet execute hybrid ranking. Future ranking must filter by application, tenant, user, collection visibility, enabled status, and policy before scoring.

