# Memory Retrieval

Memory retrieval policy is represented by `application_retrieval_policies` and memory embedding metadata.

The current implementation supports scoped memory listing and record retrieval. Semantic retrieval is schema-ready but not yet wired into context assembly.

Memory is always treated as user-context data, never as instructions and never as authorization material.

