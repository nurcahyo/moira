# Embeddings

Embedding policy is application scoped:

- provider
- model
- dimension
- batch size
- timeout
- memory/RAG enablement
- failure behavior

The schema records embedding model and dimension for memory and chunk embeddings. Embedding vectors are never returned publicly.

Rig embedding integration remains pending; no second provider SDK was introduced.

