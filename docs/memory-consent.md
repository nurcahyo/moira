# Memory Consent

Supported consent modes:

- `disabled`
- `explicit_only`
- `application_managed`
- `automatic_with_user_controls`

The implementation honors disabled/manual controls for the explicit memory API. Automatic extraction and retrieval are policy fields but not active by default.

Conversation history consent is separate from long-term memory consent.

